---
deployment:
  tasks:
    - (for p in /opt/cpanel/ea-php83/root/usr/bin/php /opt/cpanel/ea-php84/root/usr/bin/php /usr/local/bin/ea-php83 /usr/local/bin/php83 /usr/bin/php8.3; do echo "-- $p --"; test -x "$p" && "$p" -v || echo "not found/executable"; done; echo "--MultiPHP INI dir--"; ls -la /opt/cpanel/ea-php* 2>&1 | head -50) 2>&1
