---
deployment:
  tasks:
    - /opt/cpanel/ea-php83/root/usr/bin/php /home/polnutsp/app_live2/artisan tinker --execute="try { Illuminate\Support\Facades\Mail::raw('Test message from deploy diagnostic.', function (\$m) { \$m->to('sale@polnuts.pl')->subject('Polnuts mail test'); }); echo 'SENT OK'; } catch (\Throwable \$e) { echo 'FAILED: ' . get_class(\$e) . ' - ' . \$e->getMessage(); }"
